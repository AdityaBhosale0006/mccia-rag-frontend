# FastAPI backend URL (server-side only — never exposed to browser)
# For local dev: copy to .env.local and point to your local backend
# For Vercel: set BACKEND_URL in Project Settings → Environment Variables
BACKEND_URL=https://dg-chat.duckdns.org
